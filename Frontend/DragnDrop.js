class Draggin {
    
    startDrageEvent(ele) {
        ele.addEventListener('dragstart', (event) => {
            // todo
        })

        ele.addEventListener('dragover', (event) => {
            // todo
        })
    }
}